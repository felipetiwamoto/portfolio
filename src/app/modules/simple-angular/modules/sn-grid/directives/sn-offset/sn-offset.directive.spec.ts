import {SnOffsetDirective} from './sn-offset.directive'

describe('SnOffsetDirective', () => {
	it('should create an instance', () => {
		const directive = new SnOffsetDirective()
		expect(directive).toBeTruthy()
	})
})
