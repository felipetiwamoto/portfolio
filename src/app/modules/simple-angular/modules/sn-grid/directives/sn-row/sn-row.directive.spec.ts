import {SnRowDirective} from './sn-row.directive'

describe('SnRowDirective', () => {
	it('should create an instance', () => {
		const directive = new SnRowDirective()
		expect(directive).toBeTruthy()
	})
})
