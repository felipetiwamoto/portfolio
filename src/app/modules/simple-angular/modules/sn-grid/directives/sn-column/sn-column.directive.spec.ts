import {SnColumnDirective} from './sn-column.directive'

describe('SnColumnDirective', () => {
	it('should create an instance', () => {
		const directive = new SnColumnDirective()
		expect(directive).toBeTruthy()
	})
})
