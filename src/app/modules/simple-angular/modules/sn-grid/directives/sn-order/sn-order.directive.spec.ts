import {SnOrderDirective} from './sn-order.directive'

describe('SnOrderDirective', () => {
	it('should create an instance', () => {
		const directive = new SnOrderDirective()
		expect(directive).toBeTruthy()
	})
})
